# Pi coding agent extensions
# Used by setup.sh and update.sh
# Installed with: pi install npm:<package>

# MCP support (official adapter — token-efficient, handles OAuth remote servers)
pi-mcp-adapter

# LSP / diagnostics / linters injected into every edit (35+ languages)
pi-lens

# Pi ships a deliberately minimal core; the rest re-add what it left out.

# Subagents + parallel delegation
@tintinweb/pi-subagents

# Read-only /plan mode
@narumitw/pi-plan-mode

# Todo overlay that survives /reload and context compaction
@juicesharp/rpiv-todo

# Reviews recently changed code for clarity and maintainability
pi-simplify
